<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        
        html, body{
            height: 100%;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="h-full w-full bg-zinc-900 text-white p-10">
        <div class="w-10 h-10 flex items-center justify-center hover:border-2 hover:border-zinc-500 rounded-md left-2 absolute top-5">
            <a href="/home/<%= user.id %>" class="text-2xl"> <- </a>
        </div>
        <div class="m-5">
            <div class="flex gap-3 w-full ">
                <div class="h-[40px] w-[40px] ">
                    <img src="<%= user.image %>" alt="" class="object-cover h-full w-full overflow-hidden rounded-md">
                </div>
                <h1 class="text-3xl mb-3">Hey, <%= user.name %></h1>
            </div>
            <form action="/post/<%= user.id%>" method="post">
                <textarea name="content" id="" class="w-96 h-40 border-2 border-zinc-800 resize-none outline-none block p-3 rounded-md" placeholder="What's on your mind ?"></textarea>
                <input type="submit" value="Post" class="bg-yellow-400 w-55 p-2.5 mt-3 rounded-lg block text-2xl text-black">
            </form>
        </div>
    </div>
</body>
</html>